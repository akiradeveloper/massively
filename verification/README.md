# Verification

This directory collects verification and research artifacts separately from
the publishable `massively` crate.

- [`massively/`](massively/) contains the independent CPU oracle and Massively
  Core Lean proof.
- [`traversal-algebra/`](traversal-algebra/) contains its CPU oracle, complete
  graph algorithms, and Lean proof.
